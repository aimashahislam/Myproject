<%= form_for([@author, @book]) do |f| %>
  <%=   %>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :published_at %><br />
    <%= f.date_select :published_at %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>